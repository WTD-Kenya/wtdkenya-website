name: Sync to GitLab

on:
  push:
    branches:
      - main

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Push to GitLab
        run: |
          git config --global user.email "jumalawrence98@gmail.com"
          git config --global user.name "jumalaw98"
          git remote add gitlab https://oauth2:${{ secrets.glpat-TdMVHwGz4XyzBM-Xddti }}@gitlab.com/jumalaw98/wtd-web.git
          git push gitlab main --force 
